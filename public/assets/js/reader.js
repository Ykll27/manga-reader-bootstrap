(()=>{const reader=document.getElementById('reader');if(!reader)return;document.getElementById('toTop')?.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));const payload=()=>({provider:reader.dataset.provider,mangaId:reader.dataset.mangaId,mangaTitle:reader.dataset.mangaTitle,coverUrl:reader.dataset.cover,chapterId:reader.dataset.chapterId,chapterTitle:reader.dataset.chapterTitle,scrollPosition:Math.round(window.scrollY)});let timer;const save=()=>fetch('/api/progress',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload()),keepalive:true}).catch(()=>{});save();window.addEventListener('scroll',()=>{clearTimeout(timer);timer=setTimeout(save,1200)},{passive:true});window.addEventListener('pagehide',save)})();
