<%- include('partials/header') %>

<main class="container py-5">
  <div class="card bg-dark text-light border-warning shadow">
    <div class="card-body">
      <h1 class="h4 text-warning mb-3"><%= title || 'Mensagem' %></h1>

      <p><%= message || 'Algo aconteceu.' %></p>

      <a href="<%= backUrl || '/' %>" class="btn btn-warning">
        Voltar
      </a>
    </div>
  </div>
</main>

<%- include('partials/footer') %>
